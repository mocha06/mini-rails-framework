# frozen_string_literal: true

RSpec.describe Mini::Rails::Framework do
  it "has a version number" do
    expect(Mini::Rails::Framework::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
